# CLAUDE.md - RailwayRenaissance 项目规则

## 核心规则（最高优先级）

### 删除操作
**所有删除操作必须使用 Windows 回收站，禁止永久删除！**
- 禁止使用 `Remove-Item`、`rm -rf`、`del` 等永久删除命令
- 删除前必须列出计划并经用户确认
- 违者可能导致项目文件永久丢失（如 发行/ 和 中转站/）

### 保护目录
- **禁止删除、移动、重命名** `发行/` 和 `中转站/` 中的任何文件
- 这两个目录包含构建产物和素材中转，需保留

## 项目概述
Unity 6000.4.6f1 铁路复兴模拟 × 视觉小说游戏。详见 `AGENTS.md` 和 `docs/for-ai/`。